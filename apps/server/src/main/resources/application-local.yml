flowable:
  database-schema-update: true

logging:
  level:
    io.github.akaryc1b.approval: DEBUG

approval:
  security:
    identity:
      mode: local-headers
    management-permissions:
      enforced: true
      source: principal
