flowable:
  database-schema-update: true

logging:
  level:
    io.github.akaryc1b.approval: DEBUG

approval:
  demo:
    purchase-payment:
      enabled: ${APPROVAL_DEMO_PURCHASE_PAYMENT_ENABLED:false}
      sandbox:
        enabled: ${APPROVAL_DEMO_PAYMENT_SANDBOX_ENABLED:false}
        endpoint: ${APPROVAL_DEMO_PAYMENT_SANDBOX_ENDPOINT:http://127.0.0.1:0/payment-sandbox/v1/events}
        control-file: ${APPROVAL_DEMO_PAYMENT_SANDBOX_CONTROL_FILE:}
        status-file: ${APPROVAL_DEMO_PAYMENT_SANDBOX_STATUS_FILE:}
        business-key-prefix: ${APPROVAL_DEMO_PAYMENT_SANDBOX_BUSINESS_KEY_PREFIX:}
        purchase-order-reference-prefix: ${APPROVAL_DEMO_PAYMENT_SANDBOX_PURCHASE_ORDER_REFERENCE_PREFIX:}
        maximum-clock-skew: ${APPROVAL_DEMO_PAYMENT_SANDBOX_CLOCK_SKEW:5m}
  security:
    identity:
      mode: local-headers
    management-permissions:
      enforced: true
