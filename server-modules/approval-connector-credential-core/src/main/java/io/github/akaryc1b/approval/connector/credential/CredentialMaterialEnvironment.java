package io.github.akaryc1b.approval.connector.credential;

/**
 * Closed environment classification bound to one material request.
 */
public enum CredentialMaterialEnvironment {
    LOCAL_TEST,
    NON_PRODUCTION,
    PRODUCTION
}
