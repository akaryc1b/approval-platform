# ADR-0001: 使用长期 Monorepo

状态：Accepted

## 决策

后端、PC、移动端、共享包、连接器、部署和文档长期位于一个 Git 仓库，但保持独立构建和发布。

## 原因

审批协议、DSL 和表单 Schema 变更通常需要跨端原子修改。Monorepo 能让一个 PR 同时完成后端、前端、移动端、SDK、测试和文档，减少版本漂移。

## 约束

- CI 按路径触发；
- 模块依赖通过构建工具和架构测试限制；
- 客户私有代码放在外部私有扩展仓库；
- 一个仓库不意味着一个部署单元。
