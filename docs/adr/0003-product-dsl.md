# ADR-0003: 产品模型使用 Approval DSL

状态：Accepted

## 决策

设计器、版本管理和 API 使用平台自有 Approval DSL。BPMN 和 DMN 由编译器生成并视为部署产物。

## 原因

中国式审批中的前后加签、代理、无审批人策略、连续主管和退回重走策略不能稳定映射为用户直接编辑的 BPMN。自有 DSL 允许静态检查、兼容迁移、模拟和未来引擎替换。
