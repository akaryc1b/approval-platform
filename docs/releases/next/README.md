# Next Release Candidate Workspace

Status: `NOT_A_RELEASE`

此目录只允许存放下一次发布候选材料，例如待冻结的 release notes、compatibility snapshot、operations snapshot 和 manifest 草案。

它不绑定 tag，不对应 GitHub Release，不授权部署，也不构成 Production Supported 声明。

当真实发布完成后，应通过发布流程把经过验证的内容复制并冻结到 `docs/releases/<tag>/`，而不是把 `next/` 直接称为版本。
