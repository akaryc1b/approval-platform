diff --git a/server-modules/approval-application/src/main/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationService.java b/server-modules/approval-application/src/main/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationService.java
--- a/server-modules/approval-application/src/main/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationService.java
+++ b/server-modules/approval-application/src/main/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationService.java
@@ -125,7 +125,7 @@ public final class ApprovalBatchSimulationService {
         ApprovalDefinitionSimulator.SimulationResult result;
@@
         List<String> visited = result.steps().stream()
-            .map(ApprovalDefinitionSimulator.Step::nodeId)
+            .map(ApprovalDefinitionSimulator.SimulationStep::nodeId)
             .distinct()
             .sorted()
             .toList();
@@ -320,7 +320,7 @@ public final class ApprovalBatchSimulationService {
-            for (ApprovalDefinitionSimulator.Step step : scenario.steps()) {
+            for (ApprovalDefinitionSimulator.SimulationStep step : scenario.steps()) {
                 coveredNodes.add(step.nodeId());
@@ -579,7 +579,7 @@ public final class ApprovalBatchSimulationService {
-        List<ApprovalDefinitionSimulator.Step> steps,
+        List<ApprovalDefinitionSimulator.SimulationStep> steps,
         List<String> issueCodes,
diff --git a/server-modules/approval-application/src/test/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationServiceTest.java b/server-modules/approval-application/src/test/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationServiceTest.java
--- a/server-modules/approval-application/src/test/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationServiceTest.java
+++ b/server-modules/approval-application/src/test/java/io/github/akaryc1b/approval/application/ApprovalBatchSimulationServiceTest.java
@@ -45,7 +45,7 @@ class ApprovalBatchSimulationServiceTest {
         TestFixture fixture = fixture(
             PurchasePaymentTemplate.processDefinition(),
             PurchasePaymentTemplate.formDefinition(),
-            PurchasePaymentTemplate.uiSchemaDefinition()
+            purchasePaymentUi()
         );
@@ -146,7 +146,7 @@ class ApprovalBatchSimulationServiceTest {
         TestFixture fixture = fixture(
             PurchasePaymentTemplate.processDefinition(),
             PurchasePaymentTemplate.formDefinition(),
-            PurchasePaymentTemplate.uiSchemaDefinition()
+            purchasePaymentUi()
         );
@@ -229,7 +229,7 @@ class ApprovalBatchSimulationServiceTest {
         TestFixture fixture = fixture(
             PurchasePaymentTemplate.processDefinition(),
             PurchasePaymentTemplate.formDefinition(),
-            PurchasePaymentTemplate.uiSchemaDefinition()
+            purchasePaymentUi()
         );
@@ -331,6 +331,20 @@ class ApprovalBatchSimulationServiceTest {
         return new TestFixture(draft, service);
     }
 
+    private static UiSchemaDefinition purchasePaymentUi() {
+        FormDefinition form = PurchasePaymentTemplate.formDefinition();
+        return new UiSchemaDefinition(
+            UiSchemaDefinition.CURRENT_SCHEMA_VERSION,
+            form.formKey(),
+            form.version(),
+            1,
+            "Purchase payment batch UI",
+            List.of(),
+            List.of()
+        );
+    }
+
     private static ApprovalDefinition parallelDefinition() {
